---
bbbackend__mongodb_version: 3.4

# do not touch if you don't know what you are doing!
bbbackend__mongodb_apt_repo: "deb [ arch=amd64,arm64 ] http://repo.mongodb.org/apt/ubuntu/ {{ ansible_distribution_release | lower }}/mongodb-org/{{ bbbackend__mongodb_version }} multiverse"
bbbackend__mongodb_state: 'present'
